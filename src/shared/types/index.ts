/**
 * @shared/types — public API for all domain types and IPC contracts.
 *
 * Import from here instead of the individual files:
 *   import { User, Product, Shift } from '@shared/types';
 */
export * from './models';
export * from './ipc';
